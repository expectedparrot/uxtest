# Post-Login Workflow Testing

Use this study type to test authenticated product workflows. Prefer
`setup_steps` and `auth_state` so the run can start from a reliable test account
or role-specific session.

